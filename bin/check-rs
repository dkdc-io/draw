#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."

echo "Checking Rust formatting..."
cargo fmt -- --check

echo "Running clippy..."
cargo clippy --features webapp -- -D warnings

echo "Running Rust tests..."
cargo test --features webapp

echo "Rust checks passed!"
