#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."

echo "Installing draw CLI..."
cargo install --path crates/draw-cli --features webapp --force

echo "Rust install complete!"
